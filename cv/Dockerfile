FROM python:3.12-slim

WORKDIR /app

ENV PYTHONDONTWRITEBYTECODE=1
ENV PYTHONUNBUFFERED=1

CMD ["python", "-c", "import os, time; print(f'AeroVision CV worker placeholder: processing not implemented in Phase 1. CV_DEVICE={os.getenv(\"CV_DEVICE\", \"cpu\")}', flush=True); time.sleep(10**9)"]

